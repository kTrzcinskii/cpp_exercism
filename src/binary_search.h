#pragma once
#if !defined(BINARY_SEARCH_H)
#define BINARY_SEARCH_H

#include <string>
#include <vector>

namespace binary_search {
	size_t find(std::vector<int> data, int n);
}  // namespace binary_search

#endif // BINARY_SEARCH_H